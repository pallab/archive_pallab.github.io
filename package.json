{
  "name": "pallabkalita.github.io",
  "version": "1.0.0",
  "description": "My Notes",
  "author": "pallab",
  "private": true,
  "scripts": {
    "dev": "nuxt",
    "build": "nuxt build",
    "start": "nuxt start",
    "generate": "nuxt generate",
    "pbuild": "npm run build ; npm run start",
    "commit" : "git add .; git commit -m'wip'; git push",
    "deploy": "API_HOST=pallabkalita.github.io; npm run generate; npm run commit ;git subtree push --prefix dist origin master"
  },
  "dependencies": {
    "@nuxtjs/axios": "^5.3.6",
    "@nuxtjs/bulma": "^1.2.1",
    "@nuxtjs/pwa": "^3.0.0-0",
    "nuxt": "^2.0.0"
  },
  "devDependencies": {
    "@nuxt/typescript-build": "^0.6.0"
  }
}
